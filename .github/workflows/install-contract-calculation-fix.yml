name: Install contract calculation fix

on:
  push:
    branches: [master]
    paths:
      - '.github/workflows/install-contract-calculation-fix.yml'

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: master
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply contract arithmetic repair
        run: python scripts/patch-contract-calculations.py
      - name: Verify application
        run: |
          npm ci
          npm run check
      - name: Commit repair
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/components/BusinessAdminPanel.tsx api/proxy.js
          if git diff --cached --quiet; then
            echo 'No changes to commit'
          else
            git commit -m 'Fix contract totals and payment arithmetic'
            git push origin master
          fi
